From 79aafb67626c25f3c237928d611c3f6f3c111838 Mon Sep 17 00:00:00 2001
From: OmniPaper <omnipaper@omnipaper.io>
Date: Thu, 11 Jun 2026 08:50:24 +0000
Subject: [PATCH] OmniPaper - PlayerInventoryOverflowEvent

---
 .../player/PlayerInventoryOverflowEvent.java  | 48 +++++++++++++++++++
 .../java/org/bukkit/event/HandlerList.java    |  3 ++
 2 files changed, 51 insertions(+)
 create mode 100644 src/main/java/io/github/omnipaper/api/event/player/PlayerInventoryOverflowEvent.java

diff --git a/src/main/java/io/github/omnipaper/api/event/player/PlayerInventoryOverflowEvent.java b/src/main/java/io/github/omnipaper/api/event/player/PlayerInventoryOverflowEvent.java
new file mode 100644
index 0000000..e4faad6
--- /dev/null
+++ b/src/main/java/io/github/omnipaper/api/event/player/PlayerInventoryOverflowEvent.java
@@ -0,0 +1,48 @@
+package io.github.omnipaper.api.event.player;
+
+import org.bukkit.entity.Player;
+import org.bukkit.event.HandlerList;
+import org.bukkit.event.player.PlayerEvent;
+import org.bukkit.inventory.Inventory;
+import org.bukkit.inventory.ItemStack;
+import org.jspecify.annotations.NullMarked;
+
+import java.util.Map;
+
+/**
+ * Called when a plugin uses {@link Inventory#addItem(ItemStack...)} to add items
+ * into player's inventory and the inventory becomes full.
+ */
+@NullMarked
+public class PlayerInventoryOverflowEvent extends PlayerEvent {
+
+    private static final HandlerList HANDLER_LIST = new HandlerList();
+
+    public static short isListeningInvOverflowCached = -1;
+
+    private final Inventory inventory;
+    private final Map<Integer, ItemStack> overflowItemStacks;
+
+    public PlayerInventoryOverflowEvent(final Player player, final Map<Integer, ItemStack> overflowItemStacks) {
+        super(player);
+        this.inventory = player.getInventory();
+        this.overflowItemStacks = overflowItemStacks;
+    }
+
+    public Inventory getPlayerInventory() {
+        return this.inventory;
+    }
+
+    public Map<Integer, ItemStack> getOverflowItemStacks() {
+        return this.overflowItemStacks;
+    }
+
+    @Override
+    public HandlerList getHandlers() {
+        return HANDLER_LIST;
+    }
+
+    public static HandlerList getHandlerList() {
+        return HANDLER_LIST;
+    }
+}
diff --git a/src/main/java/org/bukkit/event/HandlerList.java b/src/main/java/org/bukkit/event/HandlerList.java
index ef9d9f3..86aacac 100644
--- a/src/main/java/org/bukkit/event/HandlerList.java
+++ b/src/main/java/org/bukkit/event/HandlerList.java
@@ -64,6 +64,7 @@ public class HandlerList {
                     h.handlers = null;
                 }
             }
+            io.github.omnipaper.api.event.player.PlayerInventoryOverflowEvent.isListeningInvOverflowCached = -1; // OmniPaper - PlayerInventoryOverflowEvent
         }
     }
 
@@ -77,6 +78,7 @@ public class HandlerList {
             for (HandlerList h : allLists) {
                 h.unregister(plugin);
             }
+            io.github.omnipaper.api.event.player.PlayerInventoryOverflowEvent.isListeningInvOverflowCached = -1; // OmniPaper - PlayerInventoryOverflowEvent
         }
     }
 
@@ -90,6 +92,7 @@ public class HandlerList {
             for (HandlerList h : allLists) {
                 h.unregister(listener);
             }
+            io.github.omnipaper.api.event.player.PlayerInventoryOverflowEvent.isListeningInvOverflowCached = -1; // OmniPaper - PlayerInventoryOverflowEvent
         }
     }
 
